<%= form_for @configuracao, url: form_url, method: :patch do |f| %>

  <div class="row">
    <div class=" offset-md-3 col-md-3 ">

      <div class="input-group mb-1">
        <%= f.label :duracao_consulta,:for=>"inputMDEx1" %>
        <div class="input-group-append">
        <%= f.number_field :duracao_consulta, value: @configuracao_last.duracao_consulta,:id=>"inputMDEx1" ,:class =>"form-control"%>
          <% if @configuracao_last.duracao_consulta == 1 %>
            <span class="input-group-text">hora</span>
          <% else %>
            <span class="input-group-text">horas</span>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col-md-4 ">
      <div class="input-group mb-1">
        <%= f.label :periodo_reajuste,:for=>"inputMDEx2" %>
        <div class="input-group-append">
        <%= f.text_field :periodo_reajuste, value: @configuracao_last.periodo_reajuste, :id=>"inputMDEx2" ,:class =>"form-control"%>
          <% if @configuracao_last.periodo_reajuste == 1 %>
            <span class="input-group-text">mês</span>
          <% else %>
            <span class="input-group-text">meses</span>
          <% end %>
        </div>
      </div>
    </div>

    </div>
    <br/>
    <br/>
    <div class="offset-md-2 col-md-4 offset-md-4">
      <%= f.submit 'Gravar', :class => 'btn btn-primary '%>
      <%= link_to 'Voltar', pacientes_path, :class => 'btn btn-danger ' %>
    </div>
      <% end %>
  </div>
